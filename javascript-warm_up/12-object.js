#!/usr/bin/node
const myObject = {
  type: 'object',
  value: 12
};

// Print the original object
console.log(myObject);

// Replace the value 12 with 89
myObject.value = 89;

// Print the updated object
console.log(myObject);
